/dev/comparison_out/
/data
/target
notes/Chat*
# Unsent drafts — keep local until the wording is settled.
notes/*-draft.md
.DS_Store
/tmp

# Python bytecode
__pycache__/
*.pyc

# Local-only cargo override (points wfa2lib-rs at a local fork checkout; must
# not reach CI, where that path does not exist). See .cargo/config.toml.
/.cargo/config.toml
